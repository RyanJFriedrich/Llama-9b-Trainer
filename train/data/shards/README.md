# train/data/shards/ — converted spec §6.1 shards

Output of `train/src/tools/npz_converter.py` (see `../bulk_npz/README.md`).
Each subdirectory is one spec shard (memmap arrays + sidecar.json,
fold_version v2). Contents are gitignored.

Run configs reference these directories in `data_shards:`.
